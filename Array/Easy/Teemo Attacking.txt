class Solution {
public:
    int findPoisonedDuration(vector<int>& a, int k) {
        int n=a.size();
        if(n==0)
            return 0;
        int count=0;
        for(int i=0;i<n-1;i++){
           
        count+=min(a[i+1]-a[i],k);
         
        }
        return count+k;
    }
};