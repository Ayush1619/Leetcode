class Solution {
public:
    int titleToNumber(string s) {
        long long alpha=1,res=0;
        for(int i=s.size()-1;i>=0;i--){
            res += ((s[i]-'A' + 1)*alpha);
                alpha *=26;
        }
        
        return res;
    }
};